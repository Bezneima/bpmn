<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
        <param name="atlassian-data-center-compatible">true</param>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n"  name="i18n" location="bpmn" />

    <rest key="rest" path="/bpmn" version="1.0" />


    <webwork1 key="configuration-action" class="java.lang.Object" roles-required="admin">
        <actions>
            <action name="ru.mail.jira.plugins.bpmn.controllers.TransitionsController" alias="example-page">
                <view name="success">/templates/example-view.vm</view>
            </action>
        </actions>
    </webwork1>

</atlassian-plugin>
